# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# In-browser (WASM) engine
#  - web/dualboy-web.{js,wasm}: build outputs, regenerated by web/build.sh.
#    TRACKED in git (GitHub Pages deploys them); rebuild + commit when the
#    engine changes.
#  - src/dualboy-web.{js,wasm}: local deploy copies (web/build.sh drops them
#    here for local previews). IGNORED so the desktop (Tauri) bundle, which
#    embeds DualBoy/src, never ships the web engine.
#  - src/dualboy-web-dbg.*: debug builds for core debugging only
src/dualboy-web.js
src/dualboy-web.wasm
src/dualboy-web-dbg.js
src/dualboy-web-dbg.wasm

# Local test assets (symlinks into the developer's machine)
src/roms
src/linktest.gba
